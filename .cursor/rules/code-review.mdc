---
description: 执行代码审查，检查安全性、性能和可维护性问题。适用于提交前代码审查、重构评估和代码走读场景。
globs: 
alwaysApply: false
---

# Code Review

# Code Review

## 适用场景
- 提交前代码审查（Pre-commit Review）
- 重构评估与影响面分析
- 代码走读（Code Walkthrough）
- 学习他人代码时的结构梳理

## 核心职责
1. 发现安全漏洞（如 SQL 注入、XSS、敏感信息泄露）
2. 识别性能瓶颈（如 N+1 查询、不必要的循环、内存泄漏风险）
3. 评估可维护性（如命名规范、函数长度、圈复杂度、重复代码）
4. 检查是否符合项目编码规范和最佳实践

## 审查清单

### 安全性
- [ ] 用户输入是否经过校验和转义
- [ ] 是否存在硬编码的密钥、Token 或密码
- [ ] 数据库操作是否使用参数化查询
- [ ] 文件上传是否限制类型和大小
- [ ] 敏感操作是否有权限校验

### 性能
- [ ] 是否存在明显的算法复杂度问题（如 O(n²) 嵌套循环处理大数据）
- [ ] 数据库查询是否可优化（索引、JOIN、子查询）
- [ ] 是否重复计算可缓存的结果
- [ ] I/O 操作是否可批量处理

### 可维护性
- [ ] 命名是否清晰表达意图（变量、函数、类）
- [ ] 函数是否遵循单一职责原则（建议不超过 50 行）
- [ ] 是否有适当的异常处理和日志记录
- [ ] 新增代码是否有对应的单元测试
- [ ] 注释是否解释了「为什么」而非「做了什么」

## 输出格式

对每份代码，按以下结构输出：

### 严重问题（阻塞）
| 位置 | 问题 | 风险 | 建议修复 |
|------|------|------|---------|
| 文件:行号 | 简述 | 高/中/低 | 具体修改建议 |

### 建议优化（非阻塞）
| 位置 | 建议 | 优先级 |
|------|------|--------|
| 文件:行号 | 简述 | 建议/可选 |

### 正面反馈
- 列出代码中做得好的地方，保持建设性氛围。

## 审查原则

1. **对事不对人**：评论针对代码，不针对作者。
2. **解释原因**：不仅指出问题，还要说明为什么这是个问题。
3. **提供替代方案**：如果可能，给出具体的改进代码示例。
4. **区分阻塞与非阻塞**：阻塞问题必须修复才能合并；建议优化可以后续处理。

## 示例

### 示例 1：Python 代码审查

**输入代码**：
```python
def get_user_orders(user_id):
    conn = sqlite3.connect("db.sqlite")
    cursor = conn.cursor()
    cursor.execute(f"SELECT * FROM orders WHERE user_id = {user_id}")
    return cursor.fetchall()
```

**审查输出**：

#### 严重问题
| 位置 | 问题 | 风险 | 建议修复 |
|------|------|------|---------|
| `views.py:4` | SQL 注入风险 | **高**：用户输入直接拼接进 SQL | 使用参数化查询：`cursor.execute("SELECT * FROM orders WHERE user_id = ?", (user_id,))` |
| `views.py:2` | 数据库连接未关闭 | **中**：资源泄漏 | 使用 `with` 上下文管理器或显式 `conn.close()` |

#### 建议优化
| 位置 | 建议 | 优先级 |
|------|------|--------|
| `views.py:3` | 使用 `cursor` 后未关闭 | 建议 |

## 变更日志

- 2026-04-20: 初始版本

