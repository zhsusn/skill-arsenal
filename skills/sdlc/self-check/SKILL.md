---
name: self-check
description: 当用户提到'自查'、'self-check'、'检查产出物'、'阶段完成'、'门控'或需要对当前阶段文档/代码进行质量校验时触发。执行内容一致性、完整性、交叉引用有效性和内部矛盾检查。
---

# Self-Check

产出物自查引擎。在每个阶段完成后对产出物进行系统性自查，确保质量门控通过后方可进入下一阶段。

## 适用场景

- 阶段完成后强制门控（概要需求 → 详细需求 → 设计 → 编码 → 测试 → 收尾）
- 用户主动要求检查当前文档/代码的完整性
- 发现文档间可能存在矛盾或引用失效时
- 编码完成后验证接口与 API 契约一致性
- 测试阶段验证覆盖率与测试有效性

## 核心原则

1. **零信任原则**：不假设产出物正确，每个结论必须有证据
2. **证据溯源**：每个检查结果必须标注证据位置（文件:行号 或 章节）
3. **原子化验证**：将复合声明拆分为独立可验证单元
4. **测试必须能失败**：覆盖率检查不仅看数字，还要看测试是否真正验证行为

## 执行流程

### Step 1: 上下文识别

读取以下输入：
- 当前阶段标识（从用户指令或上下文推断）
- 当前阶段产出物文件列表
- 上游依赖文档路径（前一阶段产出）
- `openspec/config.yaml` 中该阶段的 `required_sections`

### Step 2: 逐项检查

根据当前阶段自动选择检查项：

| 检查项 | 通用阶段 | 编码阶段 | 测试阶段 |
|--------|----------|----------|----------|
| 内容一致性 | ✅ | ✅ | ✅ |
| 内容完整性 | ✅ | ✅ | — |
| 交叉引用有效性 | ✅ | ✅ | — |
| 无内部矛盾 | ✅ | ✅ | — |
| 接口一致性 | — | ✅ | — |
| 覆盖率/测试有效性 | — | — | ✅ |

#### 内容一致性

对比当前产出物与上游文档，提取上游关键结论并逐项核对：
1. 在上游文档中识别所有事实性声明（如"系统面向 C 端用户"、"响应时间 < 2s"）
2. 在当前产出物中查找对应表述
3. 标记状态：一致 / 矛盾 / 缺失 / 需澄清

#### 内容完整性

对照 `config.yaml` 的 `required_sections` 检查章节完整性：
1. 读取该阶段的 `required_sections` 清单
2. 扫描产出物 Markdown 标题结构（H1/H2）
3. 检查每个 required_section 是否有对应内容（非空）
4. 检查 Mermaid 图表是否存在且语法正确

#### 交叉引用有效性

验证文档间 `@引用` 和链接的有效性：
1. 正则扫描 `@路径` 引用
2. 验证文件是否存在（支持相对路径和项目根目录解析）
3. 验证锚点（`#标题`）是否存在
4. 验证 Markdown 标准链接 `[text](path)`

#### 无内部矛盾

检测同一文档内的逻辑冲突：
1. 提取文档中所有约束、规则、数值
2. 检测同一术语的不同定义
3. 检测数值范围矛盾
4. 检测状态机转换冲突
5. 检测权限矩阵矛盾

#### 接口一致性（编码阶段）

对比代码中的接口与 `api-spec.md` / `openapi.yaml`：
1. 提取代码中的 API 定义（后端：路由装饰器；前端：API 调用函数）
2. 提取 spec 中的接口表格 / OpenAPI paths
3. 对比路径、HTTP 方法、请求参数、响应结构、异常码
4. 检查异常处理覆盖：设计中的异常场景是否都有代码处理

#### 覆盖率与测试有效性（测试阶段）

1. 覆盖率门控：运行 `pytest --cov`，检查总体覆盖率 ≥ 70%
2. 反模式检测（Green Mirage Audit）：
   - 空断言（`assert True`、`assert None is None`）
   - 同义反复（`assert len(list) == len(list)`）
   - 过度 mock（mock 了被测对象本身的核心逻辑）
   - 检查实现而非行为（直接断言内部状态）
   - 无法失败的测试（无 assert 或无条件通过）
3. 边界条件覆盖：空输入、最大值、异常值、并发场景

### Step 3: 结果聚合与门控判断

严重级别分类：
- 🔴 **BLOCKER**：必须修复，否则禁止进入下一阶段
- 🟡 **WARNING**：建议修复，可进入下一阶段但需记录风险
- 🟢 **INFO**：优化建议，不影响流程

门控规则：
- 存在 BLOCKER → 阶段状态锁定，要求修复后重新执行 self-check
- 无 BLOCKER，存在 WARNING → 允许进入下一阶段，但记录风险
- 全部通过 → 阶段状态更新为"已自查通过"

### Step 4: 输出自查报告

使用以下模板输出结构化报告：

```markdown
## 自查报告：{阶段名}

> 检查项数：{N} | 总体状态：{通过 / 存在阻塞问题 / 存在警告}

### 检查结果汇总

| 检查维度 | 通过数 | 失败数 | 警告数 | 状态 |
|----------|--------|--------|--------|------|
| 内容一致性 | {N} | {N} | {N} | {✅/❌/⚠️} |
| 内容完整性 | {N} | {N} | {N} | {✅/❌/⚠️} |
| 交叉引用有效性 | {N} | {N} | {N} | {✅/❌/⚠️} |
| 无内部矛盾 | {N} | {N} | {N} | {✅/❌/⚠️} |
| {阶段特定检查} | {N} | {N} | {N} | {✅/❌/⚠️} |

### 🔴 阻塞问题（必须修复）

1. **{问题标题}**
   - **检查项**：{content_consistency / content_completeness / ...}
   - **位置**：`{文件路径}:{行号}` 或 `{章节标题}`
   - **问题描述**：{具体描述}
   - **修复建议**：{建议}
   - **证据**：{引用原文片段}

### 🟡 警告（建议修复）

1. **{问题标题}**
   - **检查项**：{...}
   - **位置**：`{文件路径}:{行号}`
   - **问题描述**：{具体描述}
   - **修复建议**：{建议}

### ✅ 通过项（关键证据）

1. **{检查点}**
   - **证据**：`{文件路径}:{行号}` → {引用内容}

### 下一步

{根据门控规则生成下一步行动}
```

## 各阶段触发指令

| 阶段 | 指令示例 |
|------|----------|
| 阶段 1 概要需求 | 【阶段 1 自查】请检查与 proposal.md 一致性、required_sections 完整性、模块划分合理性 |
| 阶段 2 详细需求 | 【阶段 2 自查】请逐模块检查与概要需求一致性、spec/prototype/io-table/logic 完整性、模块间矛盾 |
| 阶段 3 概要设计 | 【阶段 3 自查】请检查架构覆盖 P0 模块、技术选型理由、与详细需求对应关系 |
| 阶段 4 详细设计 | 【阶段 4 自查】请检查数据库与数据架构一致性、API 与接口契约一致性、类设计覆盖功能点 |
| 阶段 5 接口驱动 | 【阶段 5 自查】请检查接口契约覆盖度、Mock 数据完整性、并行计划冲突 |
| 编码 | 【自测】请检查功能符合度、接口一致性、异常处理覆盖、单元测试通过 |
| 单元测试 | 【阶段 8 自查】请检查覆盖率≥70%、边界条件覆盖、测试独立性、反模式检测 |
| 集成测试 | 【阶段 9 自查】请检查端到端主链路覆盖、需求编号追溯、数据清理完整性 |
| 收尾 | 【最终自查】请检查所有产出物归档完整性、代码遗留问题、文档一致性 |

通用触发：`/self-check` 或 【自查】

## Gotchas

- **不自查不流转**：self-check 是阶段切换的**必要条件**，未通过 BLOCKER 时严禁进入下一阶段
- **修复后必须重查**：发现问题修复后，必须重新执行 self-check 确认问题解决，禁止凭感觉跳过
- **上游文档缺失**：若上游依赖文档不存在，标记为 BLOCKER 并要求先补全上游
- **大文档防溢出**：子检查只读取相关章节，不加载全文；必要时分段扫描
- **误报控制**：引入"需澄清"中间状态，不确定时标注为 WARNING 而非 BLOCKER，避免过度拦截
- **与 progress-tracker 联动**：self-check 通过后，progress-tracker 才更新阶段状态；self-check 只输出报告，不直接修改进度文件
- **测试有效性优先于覆盖率**：覆盖率 100% 但全是 Green Mirage（虚假信心）仍然视为 WARNING
- **不要自动修复**：self-check 只报告问题，不自动修改产出物；所有修复需经用户确认后执行
